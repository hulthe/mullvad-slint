export struct Relay {
    hostname: string,
}

export struct City {
    name: string,
    code: string,
    relays: [Relay],
}

export struct Country {
    name: string,
    code: string,
    cities: [City],
}

export global RelayList {
    in-out property <[Country]> countries;
}
